/*
scanf() 함수
   데이터 입력에 사용되는 함수
*/
#define _CRT_SECURE_NO_WAR
#include <stdio.h>

int main(void)
{
	//char character;
	//int inum;
	//float fnum;

	//scanf("%c", &character);
	//scanf("%c", &inum);
	//scanf("%c", &fnum);

	//printf("%c, %d, %f \n", character, inum, fnum);

	int num1, num2, num3;
	float fnum1, fnum2;

	//printf("게 개의 정수 입력 : ");
	//scanf("%d %d %d", &num1, &num2, &num3);
	//printf("입력된 정수들 : %d %d %d \n", num1, num2, num3);

	//printf("실수, 정수, 시수 차례대로 입력 : ");
	//scanf("%f %d %f", &fnum1, &num1, &fnum2);
	//printf("입력된 값들 : %f %d %f \n", fnum1, num1, fnum2);

}